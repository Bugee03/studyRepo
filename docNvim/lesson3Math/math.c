#include <stdio.h>
#include <math.h>

int main(){
  
  float a = sqrt(16);
  int b = pow(3,9);
  float vc = round(3.34);
  float d = ceil(3.54);
  float l = floor(3.45);
   float tir = log(3);
  
  printf("%.1f\n,%d\n,%.1f\n,%.1f\n,%.1f\n,%.1f\n",a,b,vc,d,l,tir);

}
